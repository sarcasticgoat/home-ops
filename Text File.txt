#installed fedora server 37 with minimal install on bare metal machine


### on my machine ###
sudo dnf update -y

mkdir ~/homelab
cd ~/homelab
git clone https://github.com/sarcasticgoat/test-flux-cluster
cd ./test-flux-cluster

#install required programs
#brew install age ansible flux weave-gitops go-task ipcalc jq kubectl pre-commit sops terraform yq

#install recommended
#sudo dnf install direnv helm kustomize prettier stern yamllint

sudo dnf install yubikey-manager
ssh-add -K
ssh-keygen -K
mv id_ed25519_sk_rk_general* ~/.ssh/
sudo dnf install gh
ssh -T git@github.com #tap yubikey after
git remote set-url origin git@github.com:sarcasticgoat/test-flux-cluster.git

#git config --global user.name "sarcasticgoat"


#install Homebrew
/bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"

### begin script stuff ###

brew install go-task/tap/go-task
task init

task precommit:init
task precommit:update

age-keygen -o age.agekey

mkdir -p ~/.config/sops/age
mv age.agekey ~/.config/sops/age/keys.txt

export SOPS_AGE_KEY_FILE=~/.config/sops/age/keys.txt
source ~/.bashrc

#cp .config.sample.env .config.env
#cat ~/.config/sops/age/keys.txt
#nano .config.env
#add age public key under BOOTSTRAP_AGE_PUBLIC_KEY
#get cloudflare api key

#ssh-copy-id -i ~/.ssh/id_rsa.pub root@192.168.10.210
#sudo dnf install perl-bignum

task verify
task configure
task ansible:init
task ansible:list
task ansible:ping
task ansible:prepare
#task ansible:reboot
task ansible:install
task cluster:nodes
# NAME           STATUS   ROLES                       AGE     VERSION
# k8s-0          Ready    control-plane,master      4d20h   v1.21.5+k3s1
# k8s-1          Ready    worker                    4d20h   v1.21.5+k3s1


